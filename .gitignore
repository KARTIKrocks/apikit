# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary
*.test

# Output of go coverage
*.out
coverage.html

# Go workspace
go.work
go.work.sum

# `go build .` / `go build ./...` in an examples/<name>/ directory (run
# without -o) writes a binary named after the directory, with no extension.
# Add a line here for each example directory.
examples/complete/complete
examples/openapi/openapi
examples/otel/otel

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local


next.md

playground

# Docs site: lives on the `website` branch. Checking that branch out leaves
# node_modules/ and dist/ behind here, where nothing else would ignore them.
apikit-website/
